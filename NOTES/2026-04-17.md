## DEV LOG 2026-04-17 ##
I removed all the old Grain directory as it was replaced by the new stuff which is TDPSOLA driven.

Hoping to do a cleanup and modernization of this as well as GrainMaker, although it remains unclear where RD begins and GrainMaker ends.
The true spirit of granular synthesis would be to have a single granulator that can adjust time scale seemlessly.
However the pitch synchronous aspect of what I'm calling the `GrainShifterProcessor` makes me think it should be a specialized class.
Meaning the `GrainMaker` plugin processor would own a processor graph in which a `EmissionControlProcessor` and `GrainShifterProcessor` would be different. 