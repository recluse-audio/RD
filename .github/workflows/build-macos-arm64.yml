name: Build macOS ARM64

on:
  workflow_dispatch:
    inputs:
      config:
        description: 'Build configuration'
        required: false
        default: 'Release'
        type: choice
        options:
          - Release
          - Debug

jobs:
  build:
    runs-on: macos-14  # Apple Silicon M1 runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install CMake
        run: brew install cmake

      - name: Configure
        run: |
          cmake -S . -B BUILD \
            -G Xcode \
            -DCMAKE_OSX_ARCHITECTURES=arm64

      - name: Build VST3
        run: |
          cmake --build BUILD --target RD_VST3 \
            --config ${{ github.event.inputs.config || 'Release' }}

      - name: Build Standalone
        run: |
          cmake --build BUILD --target RD_Standalone \
            --config ${{ github.event.inputs.config || 'Release' }}

      - name: Build All
        run: |
          cmake --build BUILD --target RD_All \
            --config ${{ github.event.inputs.config || 'Release' }}

      - name: Package bundles
        run: |
          CONFIG=${{ github.event.inputs.config || 'Release' }}
          ditto -c -k --sequesterRsrc --keepParent \
            "BUILD/RD_artefacts/$CONFIG/VST3/RD.vst3" \
            "RD-macOS-arm64-VST3.zip"
          ditto -c -k --sequesterRsrc --keepParent \
            "BUILD/RD_artefacts/$CONFIG/Standalone/RD.app" \
            "RD-macOS-arm64-Standalone.zip"

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: RD-macOS-arm64-VST3
          path: RD-macOS-arm64-VST3.zip

      - name: Upload Standalone
        uses: actions/upload-artifact@v4
        with:
          name: RD-macOS-arm64-Standalone
          path: RD-macOS-arm64-Standalone.zip
